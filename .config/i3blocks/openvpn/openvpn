#!/bin/bash

if [[ $(systemctl is-active openvpn) == "active" ]]; then
    echo 🔒
    show() {
        $HOME/.scripts/non-transparent-terminal -n openvpnstatus -f mono:pixelsize=18 -e $HOME/.config/i3blocks/openvpn/openvpnstatus;
    }
    case $BLOCK_BUTTON in
        1) show ;;
    esac
else
  echo ""
fi


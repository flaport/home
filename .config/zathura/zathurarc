# synctex with neovim
set synctex true
# the following setting will be overridden when zathura is launched from within neovim:
set synctex-editor-command 'st -e nvr --servername 127.0.0.1:9999 +%{line} %{input}'
# it's usually preferred to launch zathura from within vim when working with Latex files.
# To launch zathura from within vim, add the following lines to your init.vim:
# (note that it requires neovim-remove (nvr) to be installed)
# function! SyncTex()
#     exec "silent test -z $TEXBASE || TEXBASE=%:p:r; zathura --synctex-editor-command 'nvr --servername ".v:servername." +\\%{line} \\%{input}' --synctex-forward ".line(".").":".col(".").":%:p $TEXBASE.pdf &"
# endfunction
# nmap <Leader>s :call SyncTex()<CR>
# then, use the synctex keyboard shortcut <leader>+s to launch zathura from within vim.

# run external script that simulates a ctrl-click (for reverse synctex without using the mouse)
map S feedkeys ":exec ctrl-click<Return>"

# vim-like key-bindings
map u scroll half-up
map d scroll half-down
map D toggle_page_mode
map r reload
map R rotate
map J zoom in
map H zoom in
map K zoom out
map L zoom out
map i recolor
map p print
map <A-Button1> zoom in
map <A-Button3> zoom out

# settings
set statusbar-h-padding 0
set statusbar-v-padding 0
set page-padding 1

# recolor settings
set recolor-darkcolor "#dcdcdc"
set recolor-lightcolor "#1f1f1f"

# override recolor settings with theme colors
include recolor
